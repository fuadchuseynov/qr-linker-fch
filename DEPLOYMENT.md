# QR Linker - Развертывание на разных устройствах

## Быстрый старт

### 1. **Подготовка**
```bash
# Установить зависимости
bun install

# Собрать приложение
npm run build
```

---

## 📱 ANDROID (Телефон/Планшет)

### Требования:
- Android Studio
- Java Development Kit (JDK) 11+
- Android SDK

### Процесс:
```bash
# Синхронизировать проект
npm run capacitor:build

# Открыть Android Studio
npm run capacitor:android
```

**В Android Studio:**
1. Нажми Build → Build Bundle(s) / APK(s)
2. Выбери APK (для установки на телефон)
3. Скачай готовый APK файл
4. Передай на телефон и установи

---

## 🍎 iOS (iPhone/iPad)

### Требования:
- macOS
- Xcode
- Apple Developer Account (опционально)

### Процесс:
```bash
npm run capacitor:ios
```

**В Xcode:**
1. Выбери device или simulator
2. Нажми Run (▶️)
3. Для publikации → Product → Archive

---

## 💻 WINDOWS (Компьютер)

### Вариант 1: Electron (самый простой)
```bash
npm install -D electron electron-builder
```

Затем запусти:
```bash
npm run build
npx electron .
```

### Вариант 2: Portable EXE

```bash
npm run build
# Скопировать папку dist в portable папку
```

Результат: один файл .exe для запуска на Windows

---

## 📦 Распаковка и установка

### На Android:
1. Скачай APK файл
2. Включи "Неизвестные источники" в настройках
3. Открой файл и установи

### На iPhone:
1. Используй TestFlight или распределение через App Store

### На Windows:
1. Распакуй архив
2. Запусти файл .exe или index.html в браузере

---

## 🔐 Важно!

Убедись что в `.env` файле правильно указаны:
```
VITE_SUPABASE_URL=https://bnugodrnpehoywwtgqug.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

Эти ключи должны быть в каждой сборке!

---

## 🚀 Рекомендуемый способ (самый простой)

**Для всех устройств - использовать PWA (Progressive Web App):**

```bash
npm run build
# Результат в папке dist/ - это готовое приложение
# Можно открыть на любом браузере
```

Просто открой `dist/index.html` в браузере на любом устройстве!
